<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.app.springapp.mapper.ProjectMapper">

    <!-- 프로젝트 생성 (AI가 생성한 내용으로 INSERT) -->
    <insert id="insertProject" parameterType="ProjectVO"
            useGeneratedKeys="true" keyProperty="id" keyColumn="ID">
        INSERT INTO TBL_PROJECT (
            ID,
            PROJECT_TITLE,
            PROJECT_CONTENT,
            PROJECT_START_DATE,
            PROJECT_END_DATE,
            PROGRESS_DAY,
            PROJECT_CREATED_AT,
            MEMBER_ID,
            LOG_ID,
            AI_SUGGESTION_1_TITLE,
            AI_SUGGESTION_1_DESC,
            AI_SUGGESTION_2_TITLE,
            AI_SUGGESTION_2_DESC,
            AI_SUGGESTION_3_TITLE,
            AI_SUGGESTION_3_DESC,
            AI_SUGGESTION_4_TITLE,
            AI_SUGGESTION_4_DESC
        ) VALUES (
                     SEQ_PROJECT.NEXTVAL,
                     #{projectTitle},
                     #{projectContent},
                     TO_TIMESTAMP(#{projectStartDate}, 'YYYY-MM-DD'),
                     TO_TIMESTAMP(#{projectEndDate},   'YYYY-MM-DD'),
                     #{progressDay},
                     CURRENT_TIMESTAMP,
                     #{memberId},
                     #{logId},
                     #{aiSuggestion1Title},
                     #{aiSuggestion1Desc},
                     #{aiSuggestion2Title},
                     #{aiSuggestion2Desc},
                     #{aiSuggestion3Title},
                     #{aiSuggestion3Desc},
                     #{aiSuggestion4Title},
                     #{aiSuggestion4Desc}
                 )
    </insert>

    <!-- 프로젝트 단건 조회 (상세 페이지 진입 시) -->
    <select id="findById" parameterType="long" resultType="ProjectVO">
        SELECT
            TBP.ID                                                      AS id,
            TBP.PROJECT_TITLE                                           AS projectTitle,
            TBP.PROJECT_CONTENT                                         AS projectContent,
            TO_CHAR(TBP.PROJECT_START_DATE, 'YYYY-MM-DD')               AS projectStartDate,
            TO_CHAR(TBP.PROJECT_END_DATE,   'YYYY-MM-DD')               AS projectEndDate,
            TBP.PROGRESS_DAY                                            AS progressDay,
            TO_CHAR(TBP.PROJECT_CREATED_AT, 'YYYY-MM-DD HH24:MI:SS')   AS projectCreatedAt,
            TBP.MEMBER_ID                                               AS memberId,
            TBP.LOG_ID                                                  AS logId,
            TBL.VISION_TITLE                                            AS visionTitle,
            TBP.AI_SUGGESTION_1_TITLE                                   AS aiSuggestion1Title,
            TBP.AI_SUGGESTION_1_DESC                                    AS aiSuggestion1Desc,
            TBP.AI_SUGGESTION_2_TITLE                                   AS aiSuggestion2Title,
            TBP.AI_SUGGESTION_2_DESC                                    AS aiSuggestion2Desc,
            TBP.AI_SUGGESTION_3_TITLE                                   AS aiSuggestion3Title,
            TBP.AI_SUGGESTION_3_DESC                                    AS aiSuggestion3Desc,
            TBP.AI_SUGGESTION_4_TITLE                                   AS aiSuggestion4Title,
            TBP.AI_SUGGESTION_4_DESC                                    AS aiSuggestion4Desc
        FROM TBL_PROJECT TBP
                 JOIN TBL_LOG TBL ON TBP.LOG_ID = TBL.ID
        WHERE TBP.ID = #{id}
    </select>

    <!-- 내 프로젝트 목록 조회 (프로젝트 메인 페이지 캐러셀용) -->
    <select id="findAllByMemberId" parameterType="long" resultType="ProjectVO">
        SELECT
            TBP.ID                                                      AS id,
            TBP.PROJECT_TITLE                                           AS projectTitle,
            TBP.PROJECT_CONTENT                                         AS projectContent,
            TO_CHAR(TBP.PROJECT_START_DATE, 'YYYY-MM-DD')               AS projectStartDate,
            TO_CHAR(TBP.PROJECT_END_DATE,   'YYYY-MM-DD')               AS projectEndDate,
            TBP.PROGRESS_DAY                                            AS progressDay,
            TO_CHAR(TBP.PROJECT_CREATED_AT, 'YYYY-MM-DD HH24:MI:SS')   AS projectCreatedAt,
            TBP.MEMBER_ID                                               AS memberId,
            TBP.LOG_ID                                                  AS logId
        FROM TBL_PROJECT TBP
        WHERE TBP.MEMBER_ID = #{memberId}
        ORDER BY TBP.PROJECT_CREATED_AT DESC
    </select>

    <!-- 프로젝트 삭제 (프로젝트 상세 페이지에서 삭제 버튼 클릭 시) -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM TBL_PROJECT WHERE ID = #{id}
    </delete>

</mapper>